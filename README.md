🏦 Bank Super App (Backend)
Backend API для банковского суперприложения, разработанный в рамках ITU HACKATHON-2025. Приложение объединяет классический банкинг (счета, переводы) и экосистему сервисов (оплата услуг).

Роль: Backend Разработчик: Есимхан

🛠 Технический стек
Язык: Python 3.12.4


Фреймворк: FastAPI (Asynchronous) 

База данных: PostgreSQL


ORM: SQLAlchemy (AsyncMode) 


Миграции: Alembic 


Безопасность: OAuth2 (JWT Tokens), Passlib (Bcrypt) 

⚡ Функционал
Аутентификация & Безопасность:

Регистрация и Вход (JWT Token).

Хеширование паролей.

MFA (Двухфакторная аутентификация): Эмуляция СМС-кодов через консоль.

Банкинг:

Открытие счетов (автогенерация номера карты).

Просмотр баланса и карт.

Переводы (P2P):

Переводы по номеру телефона или карты.

Транзакционная целостность (ACID).

Экосистема:

Оплата сторонних сервисов (Такси, Интернет и т.д.).

История:

Полная история транзакций с фильтрацией (Приход/Расход).

🚀 Установка и Запуск
Следуйте этим шагам, чтобы запустить сервер локально.

1. Клонирование и окружение
Bash

# Клонировать репозиторий
git clone <ссылка_на_репозиторий>
cd Bank_Super_App

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Mac/Linux)
source venv/bin/activate
2. Установка зависимостей
Bash

pip install -r requirements.txt
3. Настройка переменных окружения (.env)
Создайте файл .env в корне проекта и скопируйте туда следующие настройки. Важно: Убедитесь, что PostgreSQL установлен и запущен.

Фрагмент кода

DB_USER=postgres
DB_PASSWORD=ваш_пароль_от_postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bank_db

SECRET_KEY=super_secret_hackathon_key_2025
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
4. Подготовка Базы Данных
Создайте пустую базу данных bank_db в PostgreSQL (через pgAdmin или терминал).

Примените миграции, чтобы создать таблицы:

Bash

alembic upgrade head
5. Запуск сервера
Bash

uvicorn app.main:app --reload
Сервер запустится по адресу: http://127.0.0.1:8000

📖 Документация API (Swagger)
После запуска сервера перейдите по адресу: 👉 http://127.0.0.1:8000/docs

Там доступна интерактивная документация для тестирования всех эндпоинтов.

⚠️ Важные нюансы для Frontend (React Native)
1. Авторизация (Login)
Эндпоинт /auth/login ожидает данные в формате application/x-www-form-urlencoded (Form Data), а не JSON.

Поле логина: username (сюда передавать номер телефона!).

Поле пароля: password.

2. Заголовки (Headers)
Для доступа к защищенным методам (баланс, переводы) необходимо передавать токен в заголовке: Authorization: Bearer <ваш_access_token>

3. Типы данных (Money)
Денежные суммы передаются и возвращаются как числа (float/decimal), например: 1500.00.

4. Тестирование MFA
Так как СМС-шлюз не подключен, код подтверждения для MFA выводится в консоль/терминал, где запущен сервер (Backend).

📂 Структура проекта
Plaintext

Bank_Super_App/
├── alembic/                # Файлы миграций БД
├── app/
│   ├── core/               # Конфиги и Безопасность
│   ├── db/                 # Модели БД и подключение
│   ├── routers/            # API Эндпоинты (Auth, Accounts, Transfers...)
│   ├── schemas/            # Pydantic схемы (валидация данных)
│   ├── dependencies.py     # Проверка токена (get_current_user)
│   └── main.py             # Точка входа
├── .env                    # Секретные переменные
├── alembic.ini             # Конфиг миграций
└── requirements.txt        # Список библиотек